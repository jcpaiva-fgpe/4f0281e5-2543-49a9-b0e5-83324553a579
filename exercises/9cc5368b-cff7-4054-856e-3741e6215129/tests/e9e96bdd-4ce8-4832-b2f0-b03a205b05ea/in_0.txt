select t.table_name from information_schema.tables t where t.table_schema in (select distinct s.nspname as table_schema  from pg_catalog.pg_namespace s join pg_catalog.pg_user u on u.usesysid = s.nspowner where u.usename = current_user) and t.table_type = 'BASE TABLE' order by t.table_name;