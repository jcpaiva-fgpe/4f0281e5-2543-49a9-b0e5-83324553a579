<h2><code>DROP TABLE</code></h2><p>Bir tablo artık kullanışlı olmadığında ve ona ihtiyacımız olmadığında, onu silmek uygundur; bu şekilde yer kaplamaz ve adını başka bir durumda kullanabileceğiz.</p><p>Bir tabloyu ortadan kaldırmak için <a href="https://www.postgresql komutunu kullanacağız. .org/docs/14/sql- droptable.html" target="_blank"><code>DROP TABLE</code></a>.</p><h3>Sözdizimi:</h3><blockquote>< pre><code>DROP TABLE TabloAdı [ CASCADE];</code></pre></blockquote><p>Bu ifade tabloyu verileri (satırları) dahil olmak üzere veritabanından siler. Veri Sözlüğünde bu tablodaki tüm mevcut bilgiler de silinecektir.</p><h3>Örnek:</h3><p>Eğer şu komutla bir <code>USERS</code> tablosu oluşturduysak: < blockquote><pre><code>TABLO KULLANICILARI OLUŞTUR (<br /> VARCHAR'a Giriş (25)<br /> KISITLAMA Users_log_PK BİRİNCİL ANAHTAR<br />);</code></pre></blockquote><p > Aşağıdaki komutla kaldırabiliriz:</p><blockquote><pre><code>DROP TABLE USERS;</code></pre></blockquote><p>Bu komutu kullanırken dikkatli olun, bir tablo geri döndürülemez ve yürütülmeden önce herhangi bir onay talebi yoktur.</p><h3>Alıştırma:</h3><p>Tablo oluşturma alıştırmalarından birinde, tablonun oluşturulmasını istedik Modüller:</p><h3>Alıştırma:</h3><p> ><blockquote><pre><code>TABLO MODÜLLERİ OLUŞTURMA (<br /> ad VARCHAR(100) BİRİNCİL ANAHTAR,<br /> saat INTEGER<br />);</code></pre></ blok alıntı> <p><code>MODULES</code> tablosunu nasıl silersiniz?</p>